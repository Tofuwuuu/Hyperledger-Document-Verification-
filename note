 docker compose up -d  

 Frontend

 cd Frontend
 npm run dev

 cd backend
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
python run.py


accessing schema and api
http://127.0.0.1:8000/docs

to run the container
docker compose up -d --build

If Fabric was stopped, removed, or Docker restarted and those Fabric containers are gone:

.\fabric-network\network-up.ps1
cd fabric-network
.\deploy-chaincode.ps1
cd ..
docker compose up -d --build


