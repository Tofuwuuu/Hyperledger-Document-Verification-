{
  "name": "cvsu-alumni-system",
  "version": "1.0.0",
  "description": "CVSU Carmona alumni profiles with Hyperledger Fabric document verification",
  "private": true,
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "python backend/run.py",
    "gateway": "npm --prefix fabric-gateway start",
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install-all": "npm install && npm --prefix frontend install --legacy-peer-deps && npm --prefix fabric-gateway install && python -m pip install -r backend/requirements.txt"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
