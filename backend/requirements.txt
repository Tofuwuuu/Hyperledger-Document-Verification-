fastapi==0.104.0
uvicorn==0.23.2
pymongo==4.5.0
motor==3.3.1
pydantic==2.4.2
pydantic-settings==2.0.3
python-dotenv==1.0.0
python-jose==3.3.0
passlib==1.7.4
python-multipart==0.0.6
bcrypt==4.0.1
# Temporarily commenting out fabric-sdk-py due to dependency conflict with idna package
# fabric-sdk-py==1.0.0 has a dependency on requests 2.20.0 which requires idna<2.8
# while FastAPI's anyio requires idna>=2.8
# fabric-sdk-py==1.0.0
cryptography==41.0.4
# hashlib is a built-in module, not a package
aiofiles==23.2.1
email-validator==2.0.0
qrcode==7.4.2
# Use a version with prebuilt Windows wheels to avoid build failures
pillow==10.3.0
PyJWT==2.8.0
aiohttp==3.9.3
reportlab==4.0.8

# Retry mechanism
tenacity>=8.2.2

# WebSocket support
websockets>=12.0 